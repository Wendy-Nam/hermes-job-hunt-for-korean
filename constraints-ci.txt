# CI 재현성 전용 고정 버전 — 런타임용이 아니다.
#
# requirements.txt는 하한(>=)만 선언한다(의도: 이 키트는 이미 돌고 있는 Hermes 위에 얹는
# 오버레이라, ==로 박으면 컨테이너가 이미 가진 버전과 충돌·다운그레이드를 일으킨다).
# 그래서 "런타임은 유연하게, 검증은 고정" 두 층으로 나눈다 — CI는 이 파일로 고정해서
# 업스트림이 조용히 바뀌어도 테스트 결과가 흔들리지 않게 한다.
#
# 사용: pip install -r requirements.txt -c constraints-ci.txt
# 갱신: pip freeze 로 실제 검증한 버전을 여기 반영할 것(직접 손으로 올리지 말 것).
PyYAML==6.0.3
requests==2.34.2
jieba==0.42.1
# requests 전이 의존성 — 고정하지 않으면 CI 재현성이 깨진다
certifi==2026.6.17
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0
