# 본인 것으로 커스터마이징

이건 **개인 구직 엔진**이고, 한 특정 프로필에 맞춰져 있다. 기본 상태로는 그 프로필을 타깃한다 —
*본인*에게 쓸모있게 하려면 아래 노브를 바꾸면 된다. 마법 같은 건 없다; 키워드 목록 몇 개, 정규식 몇 개, 프롬프트 몇 개다.

## 어디부터 바꿔야 하지? — 30초 지도

| 바꾸고 싶은 것 | 파일 | 언제 |
|---|---|---|
| 검색 키워드 · 연차 필터 · 제외 직군 · 채용 사이트 | `search-profile.yaml` (상단 `profile:` 블록부터) | **가장 먼저** |
| AI가 공고를 판정하는 기준 · 회사 등급(S~D) | `wiki-template/automation/job-hunting/Rules.md` | 두 번째 |
| 이력서 종류와 공고별 선택 규칙 | `wiki-template/automation/job-hunting/JobMatch.md` | 이력서 얹을 때 |
| 자동화 스케줄 · 배달 채널(디스코드) | `cron/jobs.json` | 자동화 켤 때 |
| AI 모델 · API 키 · 플러그인 | `config.example.yaml` → 본인 config | 설치 직후 |

위 순서대로 하나씩 바꾸면 된다. 아래는 각 파일의 상세 설명.

## 누구를 위해 만들어졌나 (기본 프로필)

| 항목 | 기본값 |
|---|---|
| **연차** | 저연차/주니어(0~2년). 필터가 시니어/3년+ **및 인턴·체험형**을 제외한다(제작자가 인턴 지원을 중단한 설정 — 인턴을 포함하려면 `senior_signals`에서 `intern|인턴|체험형` 토큰을 삭제). |
| **지역** | 대한민국. 보드 = 원티드·사람인·잡코리아·링크드인 KR·ATS(Greenhouse/Lever/Ashby). |
| **직무** | 두 트랙 — **세일즈**(프론트라인: SDR/BDR/AE/기술영업)·**옵스**(세일즈옵스/비즈옵스/데이터/PM). |
| **언어** | 이중언어(국문 이력서 + 외국계용 영문 이력서). |

한 줄이라도 본인과 다르면 아래 해당 노브를 고친다.

## 커스텀 노브 (우선순위 순)

### 1. 검색어·필터 — `search-profile.yaml`  ← **여기부터 (기계값의 단일 진실)**
수집기·스크래퍼가 실제로 읽는 값 전부가 이 파일 하나다:
- **`keyword_blocks`** — 실제 검색되는 키워드 블록(시노님 포함). 수집기가 런마다 한 블록씩 전 블록을 순환(지속 카운터)하니, 시노님 추가/삭제도 여기서만. 본인 타깃 직무 쿼리로 교체.
- **`title_must_match`** — 트랙 게이트. 제목이 이 정규식과 무관하면 수집 자체를 안 함(쓰레기풀 방지). 본인 직군으로 재작성.
- **`senior_signals` / `title_exp_min_years`(제목 3년+) / `body_exp_min_years`(JD본문 5년+) / `exclude_roles`(카테고리별 dict)** — 연차·무관직무 필터. 기본은 "주니어·세일즈/옵스·개발 아님". 미드/시니어면 **뒤집는다**(임계값 올리기 or `senior_signals` 비우기), 개발자면 `exclude_roles`의 dev 카테고리 삭제.
- **`ax_title` / `ax_exp_min_years` / `exclude_unless_ax`** — 특정 도메인(기본: AX/AI자동화)만 연차 컷을 완화하고, 제외 직무라도 그 도메인 맥락이면 살리는 예외 노브. 관심 도메인이 있으면 정규식만 바꿔 재사용.
- **`body_admin_signals` / `body_core_signals`** — JD 본문 2층 게이트: 어드민 신호 2종+ & 코어 신호 0이면 컷(제목만으론 못 거르는 사무성 직무 차단). 본인 직군의 "핵심 업무 신호"로 교체.
- **`track_bonus`** — 우선도 가산점(직무 트랙별 0~1.5). 선호 직무 서열을 숫자로 인코딩.
- **`boards`** — 어떤 보드가 도는지. 본인 지역 보드로 교체.

### 2. 판정·전략 문서 — `wiki-template/automation/job-hunting/Rules.md`
LLM(트리아지)이 읽는 **판정 기준**: 트랙→이력서 변형 매핑, 적합도 3단계 기준, **회사 티어(S~D — 글로벌테크/강소·외국계/일반 외국계/평범/영세, 노트 `티어` 필드)**, 세일즈/옵스 구분 원칙, 파이프라인 구조 계약. 검색어는 여기에 나열하지 않는다(yaml이 SoT). 노트의 `우선도`는 수동 채점이 아니라 **자동 파생 정렬키**다: 티어점(S5~D0)+트랙점(`track_bonus`)+적합도점(✅0.9/🔧0.5)을 `bin/priority-recalc.py`가 계산하고 수집 런마다 재계산된다(자가치유).

### 3. 보드 / 스크래퍼 — `skills/job-search/`
각 서브폴더가 하나의 보드(`wanted-search`, `kr-search`=사람인/잡코리아, `linkedin-search`, `ats-search`, `apify-search`, `web-search`). **다른 나라**면 본인 로컬 보드로 교체. **출력 계약: `--json` 플래그로 정규화 리스트 `[{"title": 직무, "company": 회사, "url": 링크}]`를 stdout에 낸다** — `job-collect.py`가 `json.loads`로 읽는다(마크다운 정규식 파싱은 폐기됨; JSON이 아니면 그 보드는 조용히 누락되지 않고 실패로 보고된다). `title`(직무)과 `company`(회사)를 뒤집으면 필터·중복판정·노트가 전부 오염되니 주의. `ats-search`는 회사 시드 목록을 읽으니 `ats-targets.json`을 본인 타깃 회사로 편집.

### 4. 판정 규칙 — `cron/jobs.json` (트리아지 잡 프롬프트)
트리아지 프롬프트가 **적합도 판정**(✅지원적합/🔧보완후지원/❌급부적합)과 "3년 이상 → ❌" 게이트를 인코딩. 게이트+트랙을 Rules.md에 맞춰 바꾼다. 또한:
- **`enabled_toolsets`** — 이미지 포스터 JD를 `vision_analyze`로 읽으려면 `"vision"` 포함.
- **`origin`** — 배달 대상(여기선 디스코드 채널). **본인** 채널/챗 id로 교체.

### 5. 이력서 매칭 — `wiki-template/automation/job-hunting/JobMatch.md`
변형 표 + 선택 규칙(여기선 국/영 × 세일즈/옵스). **본인** 이력서 변형과 직무별 선택 규칙을 넣는다. (실제 이력서 파일은 미포함 — 본인 볼트에 추가.)

### 6. 모델 / 인프라 — `config.example.yaml`
프로바이더/모델(vision 판정엔 멀티모달 모델 필요), 플러그인, 폴백 체인. 모든 `<REDACTED_SET_YOUR_OWN>`를 본인 키로.

## 검증
설정을 바꿨으면 `python3 tests/smoke_test.py`로 게이트·우선도·frontmatter 편집기가 깨지지 않았는지 확인한다.

## "내 것으로 만들기" 최소 경로
1. `search-profile.yaml`의 키워드 블록 + 필터 + 보드를 본인 것으로.
2. `Rules.md`를 본인 트랙+연차 판정 기준으로 재작성.
3. 한국이 아니면 `skills/job-search/` 스크래퍼 교체.
4. `cron/jobs.json` 트리아지 게이트 + `origin`을 본인 규칙/채널로.
5. `config.example.yaml` 시크릿 채우기.

나머지(dedup 장부·스테이징·수거·위키 구조·안전 frontmatter 편집기·vision 폴백)는 프로필 무관하게 그대로 동작한다.

## 파이프라인은 위쪽만 건드리게 계층화돼 있다
```
search-profile.yaml(검색어·필터) ─┐
Rules.md(판정 기준)               ├─ 수집+판정 크론 (아래 배관은 범용)
보드(스크래퍼)                  ──┘
      job-collect.py → .job-staging.json(대기열) → 트리아지 판정 → job-stage-commit.py → postings/ 노트
```

## 구직 파이프라인 사용법

### 🛡️ 처음 켤 때: DRY-RUN(읽기 전용)부터 — 메일 크론은 필수

메일·노트를 바꾸는 두 크론(`구직 수집+판정`, `구직 메일확인+리마인더`)은 프롬프트 맨 위에 **DRY-RUN 문단이 기본으로 들어 있다.** 그 문단이 있는 동안은 Gmail 변경(읽음·라벨·휴지통·받은편지함 제거)과 노트 변경을 **하지 않고**, 하려던 동작을 `[DRY-RUN] ...`으로 보고만 한다.

활성화에는 **독립된 두 스위치**가 있다 — 순서가 중요하다:
- **로컬 쓰기** = `.kit-live` 마커 파일(코드가 강제). 없으면 `note-set-field.py`·`job-stage-commit.py`가 쓰기를 거부하고 `[DRY-RUN]`만 낸다.
- **Gmail 변경**(읽음·라벨·휴지통) = 잡 프롬프트의 DRY-RUN 문단(프롬프트 지시뿐). 외부 서비스(Composio)라 코드가 못 막고 **되돌릴 수도 없다.** 확실히 막으려면 관찰 기간엔 Gmail을 **읽기 전용 스코프로 연결**하라 — 쓰기 도구가 스코프 부족으로 실패해 프롬프트 사고에도 메일이 안 바뀐다.

1. 크론을 켠다(`enabled: true`) — `.kit-live` 없고 DRY-RUN 문단 있음 = **완전 읽기 전용**.
2. **3~7일** 배달되는 보고를 읽고 확인한다: 회사 매칭이 맞나? 메일 분류(접수·면접·불합격)가 맞나? 판정(✅/🔧/❌)이 내 기준과 맞나?
3. **로컬 쓰기부터 켠다**: `touch /opt/data/.kit-live`(README의 그 스위치). 이제 노트·상태는 실제로 갱신되지만 **Gmail은 문단이 여전히 막는다.** 며칠 더 노트가 맞게 갱신되는지 확인한다.
4. **마지막에 Gmail을 연다(비가역·외부)**: 로컬이 정확하면 그때 잡 프롬프트의 **DRY-RUN 문단을 삭제** → 메일 읽음·라벨·휴지통 실행.

⚠️ **순서 엄수**: `.kit-live`를 켜기 전에 문단부터 지우면 **Gmail만 실행되고 로컬 노트는 계속 DRY로 남아** 메일과 위키 상태가 어긋난다. Gmail은 되돌리기 비용이 크니 반드시 로컬 검증 뒤 마지막 단계로.

### 자동 운영 (DRY-RUN 해제 후)
`구직 수집+판정` 크론이 하루 2회(08:10/20:10) 한 런에 전부 연쇄 실행한다:
1. **수거** — 직전 판정에서 ❌ 받은 노트를 위키에서 백업으로 이동
2. **수집** — `job-collect.py`(LLM 없음)가 보드 스크랩 → 제목/본문 게이트 컷 → 생존분을 스테이징 대기열에 적재
3. **판정** — LLM이 대기열의 JD를 읽고(이미지 포스터는 vision) ✅/🔧/❌ 채점 + 회사 티어(S~D) 부여
4. **입고** — `job-stage-commit.py`가 ✅/🔧만 `postings/` 노트로 생성. **공고는 판정 전엔 위키에 없다.**

결과 요약이 크론 `origin` 채널로 배달된다. 재기동 불필요 — 키워드/필터는 런마다 yaml을 다시 읽는다.

별도의 `구직 메일확인+리마인더` 크론(매일 10:00)이 지원 **이후**를 챙긴다:
1. **메일 스윕** — Composio Gmail에서 최근 24시간 채용 메일(불합격·서류합격·면접·과제·접수확인)을 검색, 회사명이 posting 노트와 **정확히 1:1 일치할 때만** `note-set-field.py`로 상태 갱신(frontmatter 통째 재작성 금지). 애매하면 노트는 안 건드리고 "📬 메일 확인 필요"로 보고만.
2. **미수집 지원건 흡수** — 접수확인 메일인데 노트가 없으면(보드 밖에서 직접 지원) `job-stage-commit.py --applied`로 상태=지원완료 노트를 생성 — 직접 지원한 것도 위키가 전부 추적한다.
3. **메일 라벨 정리(중첩 구조)** — 상위 라벨 `0-job-hunting` 아래 `applied`(접수확인 전용: 노트 반영 후 읽음+라벨+받은편지함 제거)·`interview`(면접·과제·전형 안내: 라벨만 붙이고 받은편지함 유지)·`rejected`(불합격: 노트 반영+브리핑 후 읽음+받은편지함 제거). 내용 있는 메일(불합격·면접 등)의 **요지는 리마인더 브리핑에 반드시 포함**한다(📨). 스코프는 최근 1개월(처리분은 라벨로 빠져 중복 없음).
4. **리마인더** — 갱신된 노트 기준으로 임박한 면접·마감·팔로업 후보·오늘 넣을 공고를 **티어순(S→A→B, D 제외)**으로 배달. S/A 티어는 🔧(보완후지원)도 보완 포인트와 함께 포함.

전제: Composio Gmail 연결(없으면 스윕은 스킵되고 리마인더만 동작).

### 수동 조작
```bash
# 대기열 확인 (몇 건이 판정 대기 중인가)
python3 bin/job-stage-commit.py --list

# 수동 판정 커밋 / 거부 (LLM 없이 직접)
python3 bin/job-stage-commit.py --url <공고URL> --fit "✅ 신입 가능, 트랙 일치"
python3 bin/job-stage-commit.py --url <공고URL> --reject "5년+ 요구"

# 수집만 즉시 실행 (스크랩 + 게이트 + 스테이징 적재, LLM 없음)
python3 scripts/job-collect.py

# 지원 상태 갱신 — frontmatter를 통째로 다시 쓰지 말고 반드시 이 도구로
python3 bin/note-set-field.py "wiki/automation/job-hunting/postings/<노트>.md" 상태=지원완료 세부=지원

# 직접 지원한 공고(수집 안 된 것)를 지원완료 노트로 등록 (멱등 — 이미 있으면 no-op)
python3 bin/job-stage-commit.py --applied --company "회사명" --position "포지션" --url "공고URL" --memo "7/10 접수확인"
```
판정까지 즉시 돌리고 싶으면 크론 `expr`을 잠시 당겨라(`trigger_job`은 티커가 원복시켜 안 먹힘). **버스트 후 원복 필수.**

### 상태 파일 (전부 데이터 루트, 위키 밖)
| 파일 | 역할 |
|---|---|
| `.job-seen.json` | **영구 dedup 장부** — 한 번 본/거른 URL은 재수집·재판정 안 됨 |
| `.job-staging.json` | 판정 대기열 — 수집됐지만 아직 판정 안 된 공고 |
| `.job-rejected.json` | 거부 기록(사유 포함) — 왜 걸렸는지 확인용 |
| `.job-rotation.json` | 키워드 블록 순환 카운터 — 지우면 블록 0부터 다시 |

### 자주 겪는 것
- **"수집이 0건"** — 정상일 수 있다. 장부 dedup 때문에 같은 공고는 두 번 안 들어온다. 신규 공고가 없는 회차는 원래 조용하다.
- **키워드를 추가했는데 안 긁힌다** — `keyword_blocks`는 런마다 한 블록씩 돈다. 추가한 블록 차례가 올 때까지 최대 (블록 수÷2)일. 급하면 해당 블록을 카운터가 가리키게 `.job-rotation.json`을 조정.
- **멀쩡한 공고가 게이트에 컷된다** — `.job-rejected.json`에서 사유 확인 → `title_must_match`가 너무 좁은 것. yaml을 넓히고, 이미 장부에 오른 URL은 `.job-seen.json`에서 지워야 재수집된다.
- **판정이 밀린다** — 대기열은 안전하게 쌓인다(`--list`로 확인). 다음 크론 회차가 이어서 소진한다.


## 🌐 IP 차단 우회 (원티드·유튜브가 안 될 때)

원티드(CloudFront WAF)·유튜브는 VPS의 **클라우드/데이터센터 IP를 봇으로 차단**한다(403 / RequestBlocked). 헤더로는 못 뚫고 **프록시**가 필요하다. 스크래퍼·유튜브만 프록시를 타고 LLM 호출은 직접 가도록 전용 노브를 뒀다 — `.env`에 넣고 Hermes 재시작:

```bash
# 범용 프록시(원티드·사람인·유튜브 전부 이걸로) — http://user:pass@host:port
HERMES_SCRAPER_PROXY=http://user:pass@proxy-host:port
# 유튜브 전용 최적화(선택, Webshare 쓸 때) — 위 대신/추가로
WEBSHARE_PROXY_USERNAME=xxxx
WEBSHARE_PROXY_PASSWORD=yyyy
```

- **무료로 안정적 우회는 불가능하다.** 공개 무료 프록시는 대부분 죽어 있고, 데이터센터 IP라 똑같이 차단되며, 트래픽을 가로챌 수 있어 크론 자동화엔 부적합.
- 현실적 최소: [Webshare](https://www.webshare.io) 무료 티어(가입만, 카드 X — 프록시 10개·월 1GB). 데이터센터 프록시라 **유튜브 자막엔 실효**, 원티드(한국+데이터센터 차단)는 안 될 수 있음.
- 원티드까지 확실히 뚫으려면 **한국 레지던셜 프록시**(유료, 월 몇 달러~). `HERMES_SCRAPER_PROXY`에 그 URL을 넣으면 된다.
- 프록시 없으면 현행대로 직접 연결(원티드·일부 유튜브는 차단당함, 나머지 보드는 정상).

## 🔁 같은 공고 중복 방지 (회사 별칭)

같은 회사가 다른 이름으로 올라와 중복 노트가 생기면(`당근마켓` vs `당근`, `쿠팡` vs `Coupang`) — 법인격(`(주)`·`주식회사`)·영문병기(`네이버(NAVER)`)·수식어는 이미 자동으로 합쳐진다. 그래도 안 잡히는 **진짜 별칭**은 `search-profile.yaml`의 `company_aliases`에 넣어라:

```yaml
company_aliases:
  당근: [당근마켓, Daangn, Karrot]
  토스: [비바리퍼블리카, Toss]
```

별칭들이 표준명으로 접혀 같은 공고로 인식된다. (제목/회사 **유사도 매칭은 안 쓴다** — "Sales Manager" vs "Senior Sales Manager"처럼 실제 다른 공고를 합치는 오검출 위험 때문. 명시적 별칭이 안전하다.)